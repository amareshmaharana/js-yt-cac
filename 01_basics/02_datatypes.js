"use strict"; // treat all js coe as newer version

// alert(3 + 3) -> we are using node, it can only run on browser

// datatypes:

/*
    PRIMITIVE DATA TYPES:

 -> Number
 -> String
 -> Boolean
 -> Undefined
 -> Null - type is object (imp.)
 -> Symbol
 -> Bigint


    NON-PRIMITIVE DATA TYPES:

 -> Object
 -> Array
 -> Function
*/